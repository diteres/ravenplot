# ravenplot
Installation
Run the following command for installation:
```{r}
devtools::install_github("https://github.com/diteres/ravenplot")
library(ravenplot)
```
Load the fonts once after loading the library:
```{r}
load_raven_fonts()
```
 
